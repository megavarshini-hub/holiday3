#include <stdio.h>

int main() {
    int n, i;
    int a[100]; // enough to hold 100 numbers

    scanf("%d", &n); // read number count

    for(i=0;i<n;i++)
    {
      scanf("%d", &a[i]);
      
     }
     for(i=0;i<n;i++)
     {
    printf("%d", a[i]);
     }
    return 0;

}
