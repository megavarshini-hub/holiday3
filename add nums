#include <stdio.h>

int main()
{
    int sum=0, n;
    scanf("%d", &n);
    int a[100];
  
    for(int i=0;i<n;i++) 
    {
    scanf ("%d", &a[i]);
    sum += a[i];
    }
  printf("%d", sum);
    return 0;
}
